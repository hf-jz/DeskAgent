# 习惯工程 · 审查报告 v1

> 日期：2026-07-25（编码修复）/ 2026-07-26（v1.2 增强）
> 范围：`src/main/habit/` 全部 11 个模块 + IPC/preload/渲染层集成
> 结论：三批 17 项修复全部完成并构建通过；v1.2 完成 LLM 增强与面板打磨

---

## 1. 审查发现与修复状态

### 批 1 · 阻断级（不修则功能不可用）

| 编号 | 问题 | 修复 | 状态 |
|---|---|---|---|
| A1 | 时间例程跨天统计失效：`days` 用了死代码字段 | 从每个 segment 的 `startTs` 推导本地日期；去重逻辑同步修正 | ✅ |
| A2 | `updateHabit` 列名崩溃：camelCase 直拼 SQL | 增加 camelCase→snake_case 白名单映射；证据更新路径恢复 | ✅ |
| A3 | 每日管道无调度，摘要永不生成 | 23:00 定时 + 启动时补齐昨日摘要 + `habit:run-pipeline` 手动通道；周日自动追加影子自动化 | ✅ |
| A5 | 面板无入口 | 设置页新增 Habits Tab + 托盘「暂停观察/习惯面板」菜单 | ✅ |
| A6 | 习惯未注入对话 | `agents:execute-task` 把相关习惯作为上下文随消息发给模型（原始消息不动，历史干净）；IPC 参数错位修正 | ✅ |

### 批 2 · 隐私加固

| 编号 | 问题 | 修复 | 状态 |
|---|---|---|---|
| B8 | 自管 AES 密钥，密钥即明文风险 | 改 `safeStorage`（macOS Keychain 托管密钥）；旧 AES 数据自动降级读取；加密失败 fail-closed 不落明文 | ✅ |
| B9 | 黑名单缺中文敏感词 | 补网银/支付宝/医保/密码管理等；URL 命中后回溯删除该 app 最近 60s 已落库事件 | ✅ |
| B10 | 暂停/AI 摘要状态重启丢失 | 持久化到 `deskapp-settings.json` | ✅ |
| B11 | 无知情同意门；better-sqlite3 加载失败拖垮主进程 | 未授权前采集器不启动，面板显示同意页；原生模块加载失败只禁用习惯引擎（重启时真实立功） | ✅ |

### 批 3 · 正确性 + 新功能

| 编号 | 问题/需求 | 修复/实现 | 状态 |
|---|---|---|---|
| C14 | 时区不统一（UTC 日期键错位） | 全工程统一 `localDateStr`（含渲染层） | ✅ |
| C15 | 锁屏/无窗口时误桥接 | 先结算不桥接 | ✅ |
| C16 | 习惯-消息匹配率极低 | 双向实体匹配（app/序列/URL 实体 + 词表关键词） | ✅ |
| C17 | 偏离提醒重复唠叨 | 同一习惯 4h 冷却 + 全局日上限 3 次 | ✅ |
| C20 | 采样重入 | 重入防护 | ✅ |
| D | 懂你指数 / 周报 / 例程一键执行 | v1 本地统计版上线（面板头部指数、周日自动周报、`open -a` 白名单执行） | ✅ |

## 2. v1.2 增强（2026-07-26）

| 项目 | 内容 | 状态 |
|---|---|---|
| 懂你指数 LLM 解读 | `getScoreInsight()`：AI 摘要开启时 LLM 一句话解读（每日缓存 + score 漂移 >5 刷新，每天最多 1 次调用）；关闭/失败时本地规则文本；数字永远走确定性公式 | ✅ |
| 周报 v2 | LLM 叙事（脱敏负载：泛化类别 + 计数，与每日摘要同标准）+ 本地数据附录；失败自动降级本地统计；重生成先删旧周报不堆积 | ✅ |
| 手动周报通道 | `habit:run-weekly` IPC + 简报 Tab「生成本周报告」按钮 | ✅ |
| 面板双层滚动 | SettingsApp 容器对 habit Tab 取消外层 padding/scroll，面板自滚动，单滚动条 | ✅ |

## 3. 验证记录

| 项 | 结果 |
|---|---|
| `npm run build`（nvm v24.16.0） | ✅ 通过（含 `habit:score-insight`/`habit:run-weekly` 通道注册） |
| better-sqlite3 ABI 不匹配 | ✅ B11 防护兜住未崩溃；已用 Electron 39.8.10 headers 重编 |
| HabitService initialized 日志 | ⏳ 待重启确认（/tmp/deskapp.log 为空文件，app 未在运行） |
| Habits Tab 同意页/面板渲染 | ⏳ 待重启目检 |
| 周报 LLM 路径端到端 | ⏳ 需开启 AI 摘要后用「生成本周报告」按钮验证；未开启时应落本地统计版 |

## 4. 遗留（下一轮）

- LLM 增强版依赖运行时验证：解读缓存命中率、周报叙事质量（prompt 可能需按真实输出调一轮）
- 懂你指数游戏化成长（Phase 5 亲密度外观）未做
- 飞行记录仪、HTML 周报渲染（设计方案 Phase 4 的完整形态）未做
